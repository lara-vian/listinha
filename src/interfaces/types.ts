interface IData {
  name: string,
  quantity: number
}

interface IList {
  id: number;
  name: string;
  quantity: number;
  price?: number;
}

interface ICar {
  id: number,
  name: string,
  quantity: number
  price: number
}